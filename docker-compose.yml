version: '3.8'

services:
  api:
    build: .
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - JWT_SECRET=my_super_secret_key_for_jwt_2024
      - JWT_EXPIRES_IN=1h
    restart: unless-stopped
